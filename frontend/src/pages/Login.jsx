import React,{useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {BrainCircuit} from 'lucide-react'; import {useAuth} from '../state/AuthContext';
export default function Login(){const [email,setEmail]=useState('student@demo.kz');const [password,setPassword]=useState('Demo123!');const [error,setError]=useState('');const nav=useNavigate();const {login}=useAuth();async function submit(e){e.preventDefault();try{setError('');await login(email,password);nav('/')}catch(x){setError(x.message)}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><div className="auth-logo"><BrainCircuit/><span>AdaptEd AI</span></div><h1>Welcome back</h1><p>Continue your personalized university learning path.</p>{error&&<div className="alert error">{error}</div>}<label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><button className="primary">Login</button><div className="demo-note">Demo: student@demo.kz / Demo123!</div><p className="center">No account? <Link to="/register">Create one</Link></p></form></div>}
