import React,{useEffect,useState} from 'react'; import {api} from '../api'; import {Line,Bar} from 'react-chartjs-2'; import {Chart as ChartJS,CategoryScale,LinearScale,PointElement,LineElement,BarElement,Title,Tooltip,Legend} from 'chart.js';
ChartJS.register(CategoryScale,LinearScale,PointElement,LineElement,BarElement,Title,Tooltip,Legend);
export default function Analytics(){const [d,setD]=useState(null);useEffect(()=>{api('/analytics/overview').then(setD)},[]);if(!d)return <div className="loader">Loading analytics...</div>;const common={responsive:true,maintainAspectRatio:false,scales:{y:{beginAtZero:true,max:100}}};const history={labels:d.scoreHistory.map(x=>x.label),datasets:[{label:'Quiz score',data:d.scoreHistory.map(x=>x.score),borderWidth:2,tension:.3}]};const mastery={labels:d.topicMastery.map(x=>x.topicTitle),datasets:[{label:'Mastery %',data:d.topicMastery.map(x=>x.masteryScore),borderWidth:1}]};return <><header className="page-head"><div><span className="eyebrow">LEARNING ANALYTICS</span><h1>Progress & Performance</h1><p>These statistics are generated from your quiz attempts and adaptive mastery state.</p></div></header><section className="stat-grid"><Mini label="Overall progress" value={`${d.overallProgress}%`}/><Mini label="Average score" value={`${d.averageScore}%`}/><Mini label="Mastered topics" value={d.masteredTopics}/><Mini label="Need review" value={d.reviewRequiredTopics}/></section><div className="two-col charts"><section className="card"><h2>Quiz Score History</h2><div className="chart-wrap"><Line data={history} options={common}/></div></section><section className="card"><h2>Topic Mastery</h2><div className="chart-wrap"><Bar data={mastery} options={common}/></div></section></div></>}
function Mini({label,value}){return <div className="stat-card"><div><small>{label}</small><strong>{value}</strong></div></div>}
